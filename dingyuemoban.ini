;是否开启自定义规则
enabled=false
;是否覆写之前配置的基础配置内容
overwrite_original_rules=false
;是否允许通过请求更改规则
update_ruleset_on_request=false
[common]
# 启用所有节点（不跳过不可用节点）
filter_nodes = false
# 禁用健康检查（避免自动过滤节点）
health_check = false
# 允许保留所有节点（包括无效节点）
keep_all_nodes = true
